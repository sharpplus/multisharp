<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application
        android:allowBackup="false"
        android:label="MultiSharp Bench"
        android:supportsRtl="true"
        android:theme="@android:style/Theme.Material.Light"
        android:largeHeap="true">

        <!--
          largeHeap is cheap insurance, not a correctness requirement. The whole
          N=1..1024 grid fits in 192 MB, and squeezing the desktop heap from its
          16 GB default down to that costs BOTH protocols the same 6% at N=1024,
          leaving the ratio flat to within 0.7% (24.8 / 24.7 / 24.9 / 24.8 at
          16 GB / 512 MB / 256 MB / 192 MB). So a device that grants a small heap
          does not bias the comparison; largeHeap is here only to keep a phone
          with an unusually tight per-app limit off the GC's back.
        -->

        <activity
            android:name=".BenchActivity"
            android:exported="true"
            android:screenOrientation="portrait">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>
